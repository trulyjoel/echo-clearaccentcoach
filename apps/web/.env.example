VITE_CLERK_PUBLISHABLE_KEY=

# Base URL of the deployed apps/server API. Leave empty in local dev — Vite's
# dev proxy forwards /api requests to http://localhost:3000 (see vite.config.ts).
VITE_API_URL=
